<div class="awards">
  {% if site.data.awards != blank %}
    {% assign awards_size = site.data.awards | size %}
    <div
      class="table-responsive"
      {% if include.limit and page.awards.scrollable and awards_size > 3 %}
        style="max-height: 60vw"
      {% endif %}
    >
      <table class="table table-sm table-borderless">
        {% if include.limit and page.awards.limit %}
          {% assign awards_limit = page.awards.limit %}
        {% else %}
          {% assign awards_limit = awards_size %}
        {% endif %}
        {% for item in site.data.awards limit: awards_limit %}
          <tr>
            <th scope="row" style="width: 20%">{{ item.year }}</th>
            <td>{{ item.description | emojify }}</td>
          </tr>
        {% endfor %}
      </table>
    </div>
  {% else %}
    <p>No awards so far...</p>
  {% endif %}
</div>
